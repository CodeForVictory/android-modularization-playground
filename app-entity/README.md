We don't need to have three layers (modules) for each of these entities. We should have as many modules as it made sense. We would probably need to start with domain? But the rest should be optional.
